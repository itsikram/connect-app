/** Android channel for chat/general pushes — MAX importance in pushExpo configureNotificationsChannel. */
export const ANDROID_MESSAGE_CHANNEL_ID = 'messages_high';

/** Android channel for incoming calls (Expo push + local). */
export const ANDROID_INCOMING_CALL_CHANNEL_ID = 'incoming_calls';
